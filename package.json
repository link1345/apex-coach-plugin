{
  "name": "apex-coach-plugin",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun run --cwd packages/apex-reference-mcp test && bun run --cwd packages/game-video-analysis-mcp test",
    "typecheck": "bun run --cwd packages/apex-reference-mcp typecheck && bun run --cwd packages/game-video-analysis-mcp typecheck",
    "references:validate": "bun run --cwd packages/apex-reference-mcp references:validate",
    "plugin:smoke": "bun run scripts/plugin-smoke.ts",
    "mcp:smoke": "bun run scripts/mcp-smoke.ts",
    "check": "bun run typecheck && bun run test && bun run references:validate && bun run build && bun run plugin:smoke && bun run mcp:smoke"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^26.2.0",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
